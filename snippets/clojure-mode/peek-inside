#name: peek-inside
# --
(do ;;Lets mess with running buffer
  (chord-pattern!
   nomad-chord-g
   (map
    (fn [thing-like]
      (cond
       (some #{thing-like} (degrees [$1] :minor :f4)) (degrees [$0] :minor :f3)
       :else
       [thing-like]))
    (map int (buffer-data (first (:bufs nomad-chord-g)))))))
